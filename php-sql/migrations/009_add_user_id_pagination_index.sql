-- Индекс для поддержки keyset-пагинации по заказам пользователя (только по ID).
-- Порядок колонок (user_id, id) и направление сортировки (DESC)
-- идеально соответствуют запросу, что обеспечивает максимальную производительность.
ALTER TABLE `orders` ADD INDEX `orders_user_id_id_desc_index` (`user_id`, `id` DESC);